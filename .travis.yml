language: java

jdk:
  - oraclejdk8
  - oraclejdk7

sudo: required

services:
  - docker

install: true # This runs the "true" command in the install phase; thus skipping install
script:
  - mvn verify

cache:
  directories:
    - $HOME/.m2/repository/
